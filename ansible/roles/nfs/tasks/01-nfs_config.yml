---

# Enable NFS
- name: Start NFS
  service:
    name: 'nfs-server'
    state: started
    enabled: yes
  tags:
    - nfs

# Firewalld config
- name: Firewall nfs
  firewalld:
    service: 'nfs'
    state: enabled
    permanent: true
  tags:
    - nfs

- name: Firewall rpc
  firewalld:
    service: 'rpc-bind'
    state: enabled
    permanent: true
  tags:
    - nfs

- name: Firewall mountd
  firewalld:
    service: 'mountd'
    state: enabled
    permanent: true
  tags:
    - nfs

- name: Firewalld reload service
  systemd:
    name: firewalld
    state: reloaded
  tags:
    - nfs